"use strict"; // It trate all JS code a newer version

// alert(3 + 3)
// we are not going to get anything as we are using the node engine not browser engine

let name = "sachin"; // string
console.log(typeof name);

let age = 21;       // number
console.log(typeof age);


let isLoggedIN = false; // boolean
console.log(typeof isLoggedIN);

// number --> 2^53
// bigint
// boolean --> true/false

// null --> standalone value
// It is an representation to the empty values 

// undefined --> 
let student; // This is an undefined 
console.log(student);
// we just declared the variable but not have assigned the value

// symbol --> unique

// objects 
console.log(typeof null); // we will get the type as null

